{
  "name": "chat-root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run dev",
    "build": "lerna run build",
    "start:client": "lerna run dev --scope=@chat/client",
    "start:server": "lerna run dev --scope=@chat/server"
  },
  "devDependencies": {
    "lerna": "6.0.1",
    "@chat/server": "*",
    "@chat/client": "*",
    "typescript": "^4.6.3"
  }
}
